spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/blogg?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  thymeleaf:
    cache: false
  mvc:
    hiddenmethod:
      filter:
        enabled: true
server:
  port: 8878
logging:
  level:
    com.Excalibur.dao: debug
